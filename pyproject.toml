[project]
name = "ultra-coding-agent"
version = "0.1.0"
description = "Autonomous coding agent using Claude SDK"
requires-python = "==3.12.*"
dependencies = [
    "claude-code-sdk>=0.0.25",
]

[tool.uv]
package = false
